[@saehrimnir/druidjs](../globals.md) / bray_curtis

# Function: bray_curtis()

> **bray_curtis**(`a`, `b`): `number`

Defined in: metrics/bray_curtis.js:10

Computes the Bray-Curtis distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The Bray-Curtis distance between `a` and `b`.

## See

[https://en.wikipedia.org/wiki/Bray%E2%80%93Curtis_dissimilarity](https://en.wikipedia.org/wiki/Bray%E2%80%93Curtis_dissimilarity)
