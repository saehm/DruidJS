[@saehrimnir/druidjs](../globals.md) / sokal\_michener

# Function: sokal\_michener()

> **sokal\_michener**(`a`, `b`): `number`

Defined in: [metrics/sokal\_michener.js:10](https://github.com/saehm/DruidJS/blob/dcefb767556f3c4cf43bdeb36d2750ae8d5ef93c/src/metrics/sokal_michener.js#L10)

Computes the Sokal-Michener distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The Sokal-Michener distance between `a` and `b`.
