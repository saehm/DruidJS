[@saehrimnir/druidjs](../globals.md) / sokal_michener

# Function: sokal_michener()

> **sokal_michener**(`a`, `b`): `number`

Defined in: [metrics/sokal_michener.js:10](https://github.com/saehm/DruidJS/blob/a8c3d973d427068e4ee01a4685c87a4d0f8b20c6/src/metrics/sokal_michener.js#L10)

Computes the Sokal-Michener distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The Sokal-Michener distance between `a` and `b`.
