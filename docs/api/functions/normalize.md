[@saehrimnir/druidjs](../globals.md) / normalize

# Function: normalize()

> **normalize**(`v`, `metric?`): `number`[] \| `Float64Array`\<`ArrayBufferLike`\>

Defined in: [matrix/normalize.js:14](https://github.com/saehm/DruidJS/blob/dcefb767556f3c4cf43bdeb36d2750ae8d5ef93c/src/matrix/normalize.js#L14)

Normalizes Vector `v`.

## Parameters

### v

Vector

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### metric?

[`Metric`](../type-aliases/Metric.md) = `euclidean`

## Returns

`number`[] \| `Float64Array`\<`ArrayBufferLike`\>

- The normalized vector with length 1.
