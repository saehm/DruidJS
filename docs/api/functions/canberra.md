[@saehrimnir/druidjs](../globals.md) / canberra

# Function: canberra()

> **canberra**(`a`, `b`): `number`

Defined in: [metrics/canberra.js:10](https://github.com/saehm/DruidJS/blob/a8c3d973d427068e4ee01a4685c87a4d0f8b20c6/src/metrics/canberra.js#L10)

Computes the canberra distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The canberra distance between `a` and `b`.

## See

[https://en.wikipedia.org/wiki/Canberra_distance](https://en.wikipedia.org/wiki/Canberra_distance)
