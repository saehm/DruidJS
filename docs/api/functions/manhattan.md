[@saehrimnir/druidjs](../globals.md) / manhattan

# Function: manhattan()

> **manhattan**(`a`, `b`): `number`

Defined in: [metrics/manhattan.js:9](https://github.com/saehm/DruidJS/blob/dcefb767556f3c4cf43bdeb36d2750ae8d5ef93c/src/metrics/manhattan.js#L9)

Computes the manhattan distance (`l_1`) between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The manhattan distance between `a` and `b`.
