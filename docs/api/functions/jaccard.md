[@saehrimnir/druidjs](../globals.md) / jaccard

# Function: jaccard()

> **jaccard**(`a`, `b`): `number`

Defined in: [metrics/jaccard.js:9](https://github.com/saehm/DruidJS/blob/dcefb767556f3c4cf43bdeb36d2750ae8d5ef93c/src/metrics/jaccard.js#L9)

Computes the jaccard distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The jaccard distance between `a` and `b`.
