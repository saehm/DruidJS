[@saehrimnir/druidjs](../globals.md) / jaccard

# Function: jaccard()

> **jaccard**(`a`, `b`): `number`

Defined in: [metrics/jaccard.js:9](https://github.com/saehm/DruidJS/blob/a8c3d973d427068e4ee01a4685c87a4d0f8b20c6/src/metrics/jaccard.js#L9)

Computes the jaccard distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The jaccard distance between `a` and `b`.
