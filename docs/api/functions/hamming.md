[@saehrimnir/druidjs](../globals.md) / hamming

# Function: hamming()

> **hamming**(`a`, `b`): `number`

Defined in: [metrics/hamming.js:9](https://github.com/saehm/DruidJS/blob/dcefb767556f3c4cf43bdeb36d2750ae8d5ef93c/src/metrics/hamming.js#L9)

Computes the hamming distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The hamming distance between `a` and `b`.
