[@saehrimnir/druidjs](../globals.md) / hamming

# Function: hamming()

> **hamming**(`a`, `b`): `number`

Defined in: [metrics/hamming.js:9](https://github.com/saehm/DruidJS/blob/a8c3d973d427068e4ee01a4685c87a4d0f8b20c6/src/metrics/hamming.js#L9)

Computes the hamming distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The hamming distance between `a` and `b`.
