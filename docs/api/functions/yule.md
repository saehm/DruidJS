[@saehrimnir/druidjs](../globals.md) / yule

# Function: yule()

> **yule**(`a`, `b`): `number`

Defined in: [metrics/yule.js:9](https://github.com/saehm/DruidJS/blob/dcefb767556f3c4cf43bdeb36d2750ae8d5ef93c/src/metrics/yule.js#L9)

Computes the yule distance between `a` and `b`.

## Parameters

### a

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

### b

`number`[] | `Float64Array`\<`ArrayBufferLike`\>

## Returns

`number`

The yule distance between `a` and `b`.
